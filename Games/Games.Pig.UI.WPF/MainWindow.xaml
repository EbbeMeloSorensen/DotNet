<Window x:Class="Games.Pig.UI.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Games.Pig.UI.WPF"
        mc:Ignorable="d"
        Title="Pig Game" Height="450" Width="800"
        DataContext="{Binding Path=MainWindowViewModel, Source={StaticResource BootStrapper}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Padding="0, 0, 10, 0" Text="Score:"/>
            <TextBox Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>
        </Grid>

        <StackPanel Grid.Column="1">
            <Button Width="75" Margin="10" Command="{Binding StartGameCommand}">Start Game</Button>
            <Button Width="75" Margin="10" Command="{Binding RollDieCommand}">Roll Die</Button>
            <Button Width="75" Margin="10" Command="{Binding TakePotCommand}">Take Pot</Button>
            <TextBox Width="75" Margin="10" IsReadOnly="True" />
        </StackPanel>
    </Grid>
</Window>
