<Window x:Class="Craft.ImageEditor.UI.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Craft.ImageEditor.UI.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding Path=MainWindowViewModel, Source={StaticResource BootStrapper}}">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock>
                    <Run Text="{Binding ImageEditorViewModel.MouseWorldPosition.X, StringFormat='(X,Y) = ({0}'}"/>
                    <Run Text="{Binding ImageEditorViewModel.MouseWorldPosition.Y, StringFormat=', {0})'}"/>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>

        <local:ImageEditorView DataContext="{Binding ImageEditorViewModel}"/>
    </DockPanel>
</Window>
